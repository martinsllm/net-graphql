API utilizando GraphQL e .NET Core 6

## Para rodar
Clonar o projeto e acessar o diretório da aplicação pelo terminal;
- Após isso, aplicar os seguintes comandos:
  - dotnet build;
  - dotnet watch run;
